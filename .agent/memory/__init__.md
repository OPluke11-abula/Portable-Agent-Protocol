# Memory Directory Index

This directory stores policy and strategy notes for memory handling.

- `short_term_cache.md`: session-scoped context and temporary state
- `vector_db.md`: longer-lived semantic retrieval guidance

For the runtime-facing storage schema and backend contract, start with
`.agent/memory.md`.
